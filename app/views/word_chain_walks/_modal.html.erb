
<dialog data-modal-target="modal" class=" fixed inset-0 size-auto max-h-none max-w-none bg-transparent p-0 backdrop:bg-black/70">
  <div class="flex min-h-full items-center justify-center p-4">
    <div class="w-full max-w-lg rounded-lg bg-white p-6 shadow-xl">
      <p>
        Content of the modal
      </p>

      <button type="button" data-action="modal#close" class="mt-4 rounded border px-4 py-2">
        Close modal
      </button>
    </div>
  </div>
</dialog>